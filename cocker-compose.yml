version: '3'
services:
  frontend:
    build:
      context: ./path/to/frontend
    ports:
      - "3000:3000"

  backend:
    build:
      context: ./path/to/backend
    ports:
      - "8080:8080"
    depends_on:
      - frontend
